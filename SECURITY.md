# Security Policy｜安全策略

## 支持范围

安全修复优先面向：

| 版本 | 支持状态 |
|---|---|
| `main` | 支持 |
| 最新 `0.2.x` | 支持 |
| 更早版本 | 仅在维护者决定时回补 |

## 私密报告漏洞

请不要在公开 Issue 中披露可利用漏洞、凭据或真实攻击载荷。

首选方式：

1. 打开仓库的 **Security** 页面；
2. 选择 **Report a vulnerability** 或创建私密 Security Advisory；
3. 提供受影响版本、复现条件、影响、最小无害证明和建议修复。

若私密报告入口不可用，请通过维护者 GitHub 主页联系 `@hyouo`，并只发送必要信息。不要提交模型服务密钥、第三方秘密或危险内容样本。

## 适用范围

可报告的问题包括：

- 依赖或打包链中的安全缺陷；
- CLI 对文件路径、模型输入或输出处理不安全；
- 报告生成导致秘密泄露；
- CI 权限配置过宽；
- 实验代码可意外执行不受信任内容；
- 文档给出可直接造成现实危害的操作步骤。

科学结论争议、一般 bug 和功能请求不属于安全漏洞，应使用 Issue 模板。

## 披露原则

维护者会尽量确认问题、评估影响并协调修复。修复发布前，请避免公开可利用细节。提交者可以在修复后获得致谢，但不得要求公开敏感内容。
